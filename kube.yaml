iusfgapiVersion: apps/v1
kind: Deployment
metadata:
 name: mydep123ytf
spec:
  replicas: 4
  strategy:uytd
    type: RollingUpdatehtdjyg
  selector: 
    matchLabels: hgv
      name: app
  template:
    metadata:
     name: mypod
     labels:  
       name: app
    spec: 
      containers:
       - name: docker1
         image: nginx
         ports:
           - containerPort: 80 
           
